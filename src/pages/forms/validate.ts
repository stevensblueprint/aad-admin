/*
 * JSON form validation is handled with AJV: https://github.com/ajv-validator/ajv
 * Validation Docs: https://jsonforms.io/docs/validation
 * Below is the default validator
 */

/*
import Ajv from 'ajv';
import addFormats from 'ajv-formats';

const ajv = new AJV({
  allErrors: true,
  verbose: true,
  strict: false,
  ...options,
});
addFormats(ajv);
*/
